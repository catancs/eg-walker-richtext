# EVIDENCE

> **Integrity artifact.** Every published claim below maps to a runnable, reproducible result.
> This file is generated by `scripts/gen-evidence.ts`; the generator exits non-zero (and this
> file is not trusted) if any suite test fails or the fuzzer fails.

**Overall status:** ✅ GREEN

Regenerate everything with:

```sh
npm run evidence            # builds, runs suite + fuzzer + benchmark, rewrites this file
```

## Environment

| Field | Value |
| --- | --- |
| Git SHA | `a7bd812ab47e36ca508b3a9abfb81f60834f86dd` |
| Git branch | `main` |
| Node | v25.8.1 |
| Platform | darwin / arm64 |
| CPU | Apple M4 |
| Generated (UTC) | 2026-06-08T06:59:10.718Z |
| Fuzz iterations / seed | 5000 |
| Fuzz seed bases | evidence-1, evidence-2, evidence-3 |

## 1. Claims → evidence map

The five core design claims (design doc §2) and the artifact that evidences each.

| Claim | Statement (abridged) | Evidence artifact | Status |
| --- | --- | --- | --- |
| **C1** Feasibility | Peritext-semantics inline formatting runs on an event-graph replay engine (anchor insertions inside replay + a pure order-aware resolution outside replay). | `test/adversarial.test.ts`, `test/anchors.test.ts` (§3) + the engine itself | ✅ |
| **C2** Intent preservation | Passes Peritext Examples 1–8 + failure cases P1–P4, the tombstone-capture bug (peritext#32), and Yjs's orphaned-marker bug (yjs#197). | citation-named tests in `test/adversarial.test.ts` + `test/simple-rich-doc.test.ts` oracle (§3) | ✅ |
| **C3** Memory shape | Persistent state is O(document), not O(history): snapshot `{text, spans, blocks}` with zero per-char metadata; anchors live only in the append-only log + transient replay. | `bench/bench.ts` → `bench/results.json` (§6) | ✅ |
| **C4** Convergence | All replicas converge for arbitrary concurrent histories — differentially fuzzed against an independent simple reference, seeds published. | `test/rich-fuzzer.ts` (§4) | ✅ |
| **C5** Flat-block semantics | Concurrent paragraph split/merge + text edits have specified, tested merge behavior. | `test/blocks.test.ts` (block_* tests), `test/block-convergence-regression.test.ts` (§3) | ✅ |

_Legend: ✅ reflects the suite/fuzzer run captured in this same file._

## 2. Adversarial + semantic suite

Command:

```sh
node --test --test-reporter=tap "dist/test/**/*.test.js"
```

**Totals: 44 pass / 0 fail / 1 skip** (of 45 TAP points).

Citation-named tests (`peritext_*`, `yjs_*`, `block_*`) are the "claims = test names" evidence — each is a named published failure case or semantic obligation.

### `adversarial.test.ts`

| Test | Citation | Result |
| --- | --- | --- |
| `peritext_P1_concurrent_overlap_bold` | ★ | ✅ PASS |
| `peritext_P2_toggle_counting_dog` | ★ | ✅ PASS |
| `peritext_P4_unbounded_bleed` | ★ | ✅ PASS |
| `peritext_example1_concurrent_insert_inside_span` | ★ | ✅ PASS |
| `yjs_197_orphaned_markers` | ★ | ✅ PASS |
| `peritext_table1_overlapping_comments` | ★ | ✅ PASS |
| `peritext_example4_color_lww` | ★ | ✅ PASS |
| `peritext_issue32_tombstone_capture` | ★ | ✅ PASS |

### `anchors.test.ts`

| Test | Citation | Result |
| --- | --- | --- |
| `anchors do not perturb text` |  | ✅ PASS |
| `concurrent anchors converge (item order identical)` |  | ✅ PASS |
| `delete skips anchors and deletes text` |  | ✅ PASS |
| `typing at end of bold span lands INSIDE (expanding end) [resolution]` |  | ✅ PASS |
| `typing at end of link span lands OUTSIDE (non-expanding end) [resolution]` |  | ✅ PASS |
| `typing at start of bold span lands OUTSIDE (start right-sticky) [resolution]` |  | ✅ PASS |

### `block-convergence-regression.test.ts`

| Test | Citation | Result |
| --- | --- | --- |
| `block_paragraph_start_concurrent_convergence (fuzzer seed egwrt-1-0)` | ★ | ✅ PASS |

### `blocks.test.ts`

| Test | Citation | Result |
| --- | --- | --- |
| `block_basic_split` | ★ | ✅ PASS |
| `block_concurrent_split_edit` | ★ | ✅ PASS |
| `block_same_position_split` | ★ | ✅ PASS |
| `block_merge_vs_edit_v1_semantics` | ★ | ✅ PASS |
| `block_paragraph_start_exception` | ★ | ✅ PASS |
| `block_text_insert_at_boundary_lands_in_following_block` | ★ | ✅ PASS |
| `block_paragraph_start_does_not_cross_into_previous_block` | ★ | ✅ PASS |

### `conformance-rich.test.ts`

| Test | Citation | Result |
| --- | --- | --- |
| `conformance_plaintext_ff-raw.json_via_checkoutRich` |  | ✅ PASS |
| `conformance_plaintext_am.json_via_checkoutRich` |  | ⏭️ SKIP |

### `simple-rich-doc.test.ts`

| Test | Citation | Result |
| --- | --- | --- |
| `oracle P1 concurrent overlap bold` |  | ✅ PASS |
| `oracle P2 toggle counting dog` |  | ✅ PASS |
| `oracle P4 unbounded bleed` |  | ✅ PASS |
| `oracle example1 concurrent insert inside span` |  | ✅ PASS |
| `oracle yjs197 orphaned markers` |  | ✅ PASS |
| `oracle table1 overlapping comments` |  | ✅ PASS |
| `oracle example4 color lww` |  | ✅ PASS |
| `oracle issue32 tombstone capture` |  | ✅ PASS |
| `oracle block basic split` |  | ✅ PASS |
| `oracle block concurrent split edit` |  | ✅ PASS |
| `oracle block same position split` |  | ✅ PASS |
| `oracle block delete-all v1 semantics` |  | ✅ PASS |
| `oracle boundary insert following block` |  | ✅ PASS |
| `oracle paragraph start exception` |  | ✅ PASS |

### `tombstone-variance-regression.test.ts`

| Test | Citation | Result |
| --- | --- | --- |
| `tombstone_variance_carveout verify-A-781 (text agrees, item order differs)` |  | ✅ PASS |
| `tombstone_variance_carveout verify-A-1287 (text agrees, item order differs)` |  | ✅ PASS |
| `tombstone_variance_carveout verify-B-2168 (text agrees, item order differs)` |  | ✅ PASS |
| `tombstone_variance_carveout verify-C-719 (text agrees, item order differs)` |  | ✅ PASS |
| `tombstone_variance_carveout verify-E-370 (text agrees, item order differs)` |  | ✅ PASS |
| `tombstone_variance_carveout verify-E-4513 (text agrees, item order differs)` |  | ✅ PASS |
| `tombstone_variance_carveout verify-B-2626 (text agrees, item order differs)` |  | ✅ PASS |

★ = citation-named claim test.

## 3. Differential fuzzing

The fuzzer (`test/rich-fuzzer.ts`) drives 3 replica pairs (engine oplog + an independent `SimpleRichDoc` oracle) through random local edits and pairwise/full-mesh merges, asserting engine self-convergence on `{text, spans, blocks}` and engine-vs-oracle agreement wherever the comparison is well-defined.

Command (per seed base):

```sh
FUZZ_ITERS=5000 FUZZ_SEED=<seed-base> node dist/test/rich-fuzzer.js
```

| Seed base | Iterations | Engine self-converged | Visible-text variant | Item-order variant | Carved span/block variance | Result |
| --- | --- | --- | --- | --- | --- | --- |
| `evidence-1` | 5000 | ✅ 5000/5000 | 21/5000 (0.420%) | 3/5000 (0.060%) | 1/5000 (0.020%) | ✅ PASS |
| `evidence-2` | 5000 | ✅ 5000/5000 | 13/5000 (0.260%) | 4/5000 (0.080%) | 1/5000 (0.020%) | ✅ PASS |
| `evidence-3` | 5000 | ✅ 5000/5000 | 25/5000 (0.500%) | 2/5000 (0.040%) | 0/5000 (0.000%) | ✅ PASS |

**Honest claim:** 0 unclassified mark-resolution divergences; the engine self-converged on N/N iterations for every seed base; the only carve-out is the precisely-characterized tombstone-order / equal-letter-swap variance of the side-table oracle (rates above), each instance of which is logged and is a known text-CRDT-ordering difference — never a mark or block bug.

## 4. Conformance (real trace)

`checkoutRich` reproduces the `testdata/ff-raw.json` real `dt export` trace's ground-truth `endContent`, materializing to empty spans + a single block (plain-text trace). This is asserted by `conformance_plaintext_ff-raw.json_via_checkoutRich` in `test/conformance-rich.test.ts`, which is part of the suite run in §2 above. (A second, slower `am.json` trace runs under `SLOW_TESTS=1`.)

Status in this run: ✅ PASS.

## 5. Benchmarks

Run with `node --expose-gc dist/bench/bench.js` (writes `bench/results.json`; gitignored — its data is embedded below).

**Honesty framing (copied from `bench/bench.ts`):** our engine is a *deliberately unoptimized TypeScript reference* — no run-length encoding, every op replayed in full on every checkout, naive causal traversal, and **no binary codec** (the durable artifact is `JSON.stringify(oplog)`). The **TIME** column will show us slower than Yjs/Automerge — that is expected and is *not* the claim (it measures readable-reference vs years of Rust/WASM production engineering). The **ENCODED-SIZE** column for "ours" is uncompacted JSON; the libraries ship purpose-built binary codecs. The **headline metric is MEMORY SHAPE (claim C3)**: our persistent *read* form is an O(document) resolved snapshot `{text, spans, blocks}` carrying **zero** per-character CRDT metadata, while the durable *write* form is an append-only oplog like every op-based CRDT. Every library × scenario row is printed — no cherry-picking.

### Benchmark environment

| Field | Value |
| --- | --- |
| node | v25.8.1 |
| date | 2026-06-08T06:55:02.956Z |
| cpu | Apple M4 |
| opCap | 20000 |
| flatOps | 5179 |
| estDocLen | 21362 |
| markCount | 171 |

### Results

| Library | Scenario | Time (ms) | Heap (bytes) | Encoded (bytes) | Note |
| --- | --- | ---: | ---: | ---: | --- |
| ours (reference, unoptimized TS; no binary codec) | 1-seq-plaintext | 444 | 8,488,200 | 1,014,660 | ours: oplog JSON (no binary codec in v1) |
| ours (reference, unoptimized TS; no binary codec) | 2-richtext | 455 | 8,725,096 | 1,045,041 | ours: oplog JSON (no binary codec in v1) |
| ours (reference, unoptimized TS; no binary codec) | 3-steady-snapshot-proxy | — | 9,770,160 | 32,749 | O(document) RESOLVED SNAPSHOT {text,spans,blocks}, ZERO per-char metadata (claim C3). NOT comparable to the libs binary "encoded" rows: those encode FULL HISTORY; this is a point-in-time read form. The apples-to-apples size-vs-libs row is 3-steady-oplog-json below. |
| ours (reference, unoptimized TS; no binary codec) | 3-steady-oplog-json | — | 9,770,160 | 1,045,041 | durable append-only oplog as JSON (no binary codec in v1) |
| Yjs (production) | 1-seq-plaintext | 18 | 8,366,072 | 89,700 |  |
| Yjs (production) | 2-richtext | 7 | 8,576,392 | 104,014 |  |
| Yjs (production) | 3-steady-encoded | — | 8,642,816 | 104,014 | Y.encodeStateAsUpdate (binary) |
| Automerge (production) | 1-seq-plaintext | 143 | 7,614,568 | 32,030 |  |
| Automerge (production) | 2-richtext | 22 | 7,666,296 | 35,451 |  |
| Automerge (production) | 3-steady-encoded | — | 7,666,832 | 35,451 | Automerge.save (binary) |

## 6. Reproduction

| Section | Command |
| --- | --- |
| Everything | `npm run evidence` |
| Build | `npm run build` |
| §2 Suite | `node --test --test-reporter=tap "dist/test/**/*.test.js"` |
| §3 Fuzzing | `FUZZ_ITERS=5000 FUZZ_SEED=evidence-1 node dist/test/rich-fuzzer.js` (repeat per seed base) |
| §4 Conformance (slow am.json too) | `SLOW_TESTS=1 node --test "dist/test/conformance-rich.test.js"` |
| §5 Benchmarks | `node --expose-gc dist/bench/bench.js` |

Tune the fuzz count with `EVIDENCE_FUZZ_ITERS` and seed bases with `EVIDENCE_FUZZ_SEEDS` (comma-separated) when running `npm run evidence`.

---

_Generated 2026-06-08T06:59:10.718Z from `a7bd812ab47e36ca508b3a9abfb81f60834f86dd`._
